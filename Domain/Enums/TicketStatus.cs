namespace Domain.Enums
{
    /// <summary>
    /// Représente l'état actuel d'un ticket.
    /// </summary>
    public enum TicketStatus
    {
        Open,
        InProgress,
        Resolved,
        Closed,
        Cancelled
    }
}